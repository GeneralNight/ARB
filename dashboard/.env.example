# Mesmas credenciais do robo — o painel le e escreve o mesmo banco.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Tranca do painel. SEM ISTO ELE NAO RESPONDE NADA (falha fechada de proposito):
# o painel carrega a service_role, que ignora RLS e pode tudo no banco.
DASHBOARD_USER=arb
DASHBOARD_PASSWORD=
